# Only install dependency versions published at least 7 days ago.
# Matches the dependabot cooldown (default-days: 7) and the project rule
# against freshly-published, unvetted releases.
# Applies to resolution (`npm install`); `npm ci` installs the committed
# lockfile and is unaffected. Requires npm >= 11.9.
min-release-age=7
